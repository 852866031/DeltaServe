diff --git a/python/sglang/srt/configs/__init__.py b/python/sglang/srt/configs/__init__.py
index 23e9081..36dab03 100644
--- a/python/sglang/srt/configs/__init__.py
+++ b/python/sglang/srt/configs/__init__.py
@@ -17,9 +17,19 @@ from sglang.srt.configs.kimi_linear import KimiLinearConfig
 from sglang.srt.configs.kimi_vl import KimiVLConfig
 from sglang.srt.configs.kimi_vl_moonvit import MoonViTConfig
 from sglang.srt.configs.laguna import LagunaConfig
-from sglang.srt.configs.lfm2 import Lfm2Config
-from sglang.srt.configs.lfm2_moe import Lfm2MoeConfig
-from sglang.srt.configs.lfm2_vl import Lfm2VlConfig
+# Port-only env-tolerance hack: tolerate missing transformers model configs.
+try:
+    from sglang.srt.configs.lfm2 import Lfm2Config
+except ImportError:
+    pass
+try:
+    from sglang.srt.configs.lfm2_moe import Lfm2MoeConfig
+except ImportError:
+    pass
+try:
+    from sglang.srt.configs.lfm2_vl import Lfm2VlConfig
+except ImportError:
+    pass
 from sglang.srt.configs.longcat_flash import LongcatFlashConfig
 from sglang.srt.configs.minicpmv4_6 import MiniCPMV4_6Config, MiniCPMV4_6VisionConfig
 from sglang.srt.configs.nano_nemotron_vl import (
diff --git a/python/sglang/srt/entrypoints/engine.py b/python/sglang/srt/entrypoints/engine.py
index ee84ff1..75d9375 100644
--- a/python/sglang/srt/entrypoints/engine.py
+++ b/python/sglang/srt/entrypoints/engine.py
@@ -85,6 +85,7 @@ from sglang.srt.managers.multi_tokenizer_mixin import (
     MultiTokenizerRouter,
     run_multi_detokenizer_router_process,
 )
+from sglang.srt.deltaserve.backward_process import spawn_backward_process
 from sglang.srt.managers.scheduler import run_scheduler_process
 from sglang.srt.managers.template_detection import resolve_auto_parsers
 from sglang.srt.managers.template_manager import TemplateManager
@@ -105,7 +106,7 @@ from sglang.srt.utils import (
     set_prometheus_multiproc_dir,
     set_ulimit,
 )
-from sglang.srt.utils.network import get_zmq_socket, is_port_available
+from sglang.srt.utils.network import get_open_port, get_zmq_socket, is_port_available
 from sglang.srt.utils.torch_memory_saver_adapter import TorchMemorySaverAdapter
 from sglang.srt.utils.watchdog import SubprocessWatchdog
 from sglang.version import __version__
@@ -246,6 +247,21 @@ class Engine(EngineScoreMixin, EngineBase):
             tokenizer_manager._subprocess_watchdog = subprocess_watchdog
         self.port_args = port_args
 
+        # Phase 6: optionally spawn the DeltaServe backward subprocess. The
+        # MPS env (CUDA_MPS_ACTIVE_THREAD_PERCENTAGE) is applied to the
+        # child only inside spawn_backward_process — the parent's
+        # os.environ is untouched so inference keeps the full GPU.
+        self.backward_process = None
+        if getattr(server_args, "enable_finetuning", False) and server_args.node_rank == 0:
+            bwd_port = get_open_port()
+            bwd_channel_addr = f"tcp://127.0.0.1:{bwd_port}"
+            self.backward_process = spawn_backward_process(
+                channel_addr=bwd_channel_addr,
+                model_name=server_args.model_path,
+                mps_pct=getattr(server_args, "backward_mps_percentage", 10),
+            )
+            self.backward_channel_addr = bwd_channel_addr
+
         # Initialize ZMQ sockets
         context = zmq.Context(2)
         if self.server_args.node_rank == 0:
diff --git a/python/sglang/srt/managers/io_struct.py b/python/sglang/srt/managers/io_struct.py
index ebe152f..0970164 100644
--- a/python/sglang/srt/managers/io_struct.py
+++ b/python/sglang/srt/managers/io_struct.py
@@ -189,6 +189,8 @@ class GenerateReqInput(BaseReq):
     lora_path: Optional[Union[List[Optional[str]], Optional[str]]] = None
     # The uid of LoRA adaptors, should be initialized by tokenizer manager
     lora_id: Optional[Union[List[Optional[str]], Optional[str]]] = None
+    # DeltaServe: tag this request as a finetuning sample (Phase 3 hook).
+    is_finetuning: bool = False
 
     # Custom logit processor for advanced sampling control. Must be a serialized instance
     # of `CustomLogitProcessor` in python/sglang/srt/sampling/custom_logit_processor.py
@@ -789,6 +791,11 @@ class TokenizedGenerateReqInput(BaseReq):
     # Whether to return entropy
     return_entropy: bool = False
 
+    # DeltaServe: whether this request is a finetuning-tagged sample.
+    # When True, the runner captures per-sample activations and routes through
+    # BreakableCudaGraphRunner instead of the standard cuda-graph path.
+    is_finetuning: bool = False
+
     token_type_ids: Optional[List[int]] = None
 
     need_wait_for_mm_inputs: bool = False
diff --git a/python/sglang/srt/managers/schedule_batch.py b/python/sglang/srt/managers/schedule_batch.py
index ce81a8c..acbbba0 100755
--- a/python/sglang/srt/managers/schedule_batch.py
+++ b/python/sglang/srt/managers/schedule_batch.py
@@ -690,6 +690,9 @@ class Req(ReqDllmMixin):
     ):
         # Input and output info
         self.rid = rid
+        # DeltaServe Phase 3: finetuning tag (set True by Scheduler when the
+        # incoming GenerateReqInput / TokenizedGenerateReqInput carries it).
+        self.is_finetuning: bool = False
         self.origin_input_ids_unpadded = array(
             "q", origin_input_ids_unpadded or origin_input_ids
         )  # Before image padding
diff --git a/python/sglang/srt/managers/scheduler.py b/python/sglang/srt/managers/scheduler.py
index 4c5561e..6cef317 100644
--- a/python/sglang/srt/managers/scheduler.py
+++ b/python/sglang/srt/managers/scheduler.py
@@ -40,6 +40,7 @@ from torch.distributed import barrier
 
 from sglang.jit_kernel.ngram_embedding import update_token_table
 from sglang.srt.configs.model_config import ModelConfig, ModelImpl
+from sglang.srt.configs.finetune import FinetuneConfig
 from sglang.srt.constrained.grammar_manager import GrammarManager
 from sglang.srt.disaggregation.decode import (
     DecodePreallocQueue,
@@ -304,6 +305,7 @@ class Scheduler(
         attn_cp_rank: int,
         moe_dp_rank: int,
         dp_rank: Optional[int],
+        finetune_config: Optional[FinetuneConfig] = None,
     ):
         self.is_initializing = True
         # init_soft_watchdog starts a daemon thread that reads these on its first tick.
@@ -313,6 +315,22 @@ class Scheduler(
 
         # Parse args
         self.server_args = server_args
+        self.finetune_config = finetune_config
+        if finetune_config is not None and finetune_config.enable_finetuning:
+            from sglang.srt.managers.scheduler_components.finetune_scheduler_mixin import (
+                FinetuneSchedulerMixin,
+            )
+            from sglang.srt.managers.scheduler_components.finetune_coordinator import (
+                FinetuneCoordinator,
+            )
+            self.__class__ = type(
+                "FinetuneScheduler", (FinetuneSchedulerMixin, self.__class__), {}
+            )
+            self.finetune_coordinator = FinetuneCoordinator(
+                finetune_config, backward_channel=None
+            )
+        else:
+            self.finetune_coordinator = None
         self.nccl_port = port_args.nccl_port
         self.schedule_policy = server_args.schedule_policy
         self.enable_priority_scheduling = server_args.enable_priority_scheduling
@@ -1855,6 +1873,12 @@ class Scheduler(
             )
             req.tokenizer = self.tokenizer
 
+            # DeltaServe Phase 3: propagate finetuning tag onto the Req.
+            # Existing lora_path / lora_id plumbing handles adapter loading;
+            # this is purely a tag for downstream FT-aware scheduling.
+            if getattr(recv_req, "is_finetuning", False):
+                req.is_finetuning = True
+
             if self.disaggregation_mode != DisaggregationMode.NULL:
                 # Invalid request for disaggregated mode
                 if (
diff --git a/python/sglang/srt/managers/tokenizer_manager.py b/python/sglang/srt/managers/tokenizer_manager.py
index cae9ed0..52a7fa2 100644
--- a/python/sglang/srt/managers/tokenizer_manager.py
+++ b/python/sglang/srt/managers/tokenizer_manager.py
@@ -1067,6 +1067,7 @@ class TokenizerManager(TokenizerControlMixin, TokenizerManagerScoreMixin):
                 need_wait_for_mm_inputs=obj.need_wait_for_mm_inputs,
                 num_items_assigned=obj.num_items_assigned,
                 multi_item_delimiter_indices=obj.multi_item_delimiter_indices,
+                is_finetuning=obj.is_finetuning,
             )
         elif isinstance(obj, EmbeddingReqInput):
             # Resolve unresolved embed overrides now that input_ids are available
diff --git a/python/sglang/srt/mem_cache/allocator.py b/python/sglang/srt/mem_cache/allocator.py
index 7ec8bd2..43e6ccc 100755
--- a/python/sglang/srt/mem_cache/allocator.py
+++ b/python/sglang/srt/mem_cache/allocator.py
@@ -20,7 +20,7 @@ Page-aligned memory pool.
 """
 
 import abc
-from typing import TYPE_CHECKING
+from typing import Callable, TYPE_CHECKING
 
 import torch
 import triton
@@ -55,6 +55,16 @@ class BaseTokenToKVPoolAllocator(abc.ABC):
         self.is_not_in_free_group = True
         self.free_group = []
 
+        # DeltaServe FinetuningStore hook: predicate used by alloc() to skip
+        # indices reserved for FT samples. Fast path is gated on
+        # _has_reserved so default behavior is byte-identical when unused.
+        self._reserved_pred: Callable[[int], bool] = lambda i: False
+        self._has_reserved: bool = False
+
+    def set_reserved_predicate(self, pred: Callable[[int], bool]) -> None:
+        self._reserved_pred = pred
+        self._has_reserved = True
+
     @property
     def size_full(self):
         return self.size
@@ -152,8 +162,25 @@ class TokenToKVPoolAllocator(BaseTokenToKVPoolAllocator):
         if need_size > len(self.free_pages):
             return None
 
-        select_index = self.free_pages[:need_size]
-        self.free_pages = self.free_pages[need_size:]
+        if not self._has_reserved:
+            select_index = self.free_pages[:need_size]
+            self.free_pages = self.free_pages[need_size:]
+            return select_index
+
+        # FT-reserved predicate active: filter indices out of the alloc
+        # candidates but keep them in free_pages (still owned by FT).
+        free_list = self.free_pages.tolist()
+        reserved_mask = torch.tensor(
+            [self._reserved_pred(int(i)) for i in free_list],
+            dtype=torch.bool,
+            device=self.free_pages.device,
+        )
+        kept = self.free_pages[~reserved_mask]
+        if kept.numel() < need_size:
+            return None
+        select_index = kept[:need_size]
+        reserved_in_pool = self.free_pages[reserved_mask]
+        self.free_pages = torch.cat([kept[need_size:], reserved_in_pool])
         return select_index
 
     def free(self, free_index: torch.Tensor):
diff --git a/python/sglang/srt/model_executor/forward_batch_info.py b/python/sglang/srt/model_executor/forward_batch_info.py
index 6698429..6b02c12 100644
--- a/python/sglang/srt/model_executor/forward_batch_info.py
+++ b/python/sglang/srt/model_executor/forward_batch_info.py
@@ -430,6 +430,12 @@ class ForwardBatch(ForwardBatchDeepSeekMHAMixin):
     # For dumper: request IDs for cross-step sequence tracking
     rids: Optional[List[str]] = None
 
+    # DeltaServe: per-row bool tensor marking finetuning-tagged samples.
+    # None on inference-only batches (preserves the standard cuda-graph fast
+    # path). When set with any True row, model_runner routes through
+    # BreakableCudaGraphRunner so per-sample activation hooks can fire.
+    is_finetuning_mask: Optional[torch.Tensor] = None
+
     @classmethod
     def init_new(
         cls,
@@ -541,6 +547,15 @@ class ForwardBatch(ForwardBatchDeepSeekMHAMixin):
 
         device = model_runner.device
 
+        # DeltaServe: populate the FT mask only when at least one req is
+        # finetuning-tagged. Inference-only batches leave the field as None
+        # so the standard cuda-graph fast path is byte-identical.
+        if any(getattr(req, "is_finetuning", False) for req in batch.reqs):
+            ret.is_finetuning_mask = torch.tensor(
+                [bool(getattr(req, "is_finetuning", False)) for req in batch.reqs],
+                dtype=torch.bool,
+            ).to(device, non_blocking=True)
+
         if batch.extend_input_logprob_token_ids is not None:
             ret.extend_input_logprob_token_ids_gpu = (
                 batch.extend_input_logprob_token_ids.to(device, non_blocking=True)
diff --git a/python/sglang/srt/model_executor/model_runner.py b/python/sglang/srt/model_executor/model_runner.py
index d8245c0..b752dd0 100644
--- a/python/sglang/srt/model_executor/model_runner.py
+++ b/python/sglang/srt/model_executor/model_runner.py
@@ -3274,6 +3274,23 @@ class ModelRunner(ModelRunnerKVCacheMixin):
             if self.is_hybrid_swa:
                 self.token_to_kv_pool.invalidate_loc_cache()
 
+            # DeltaServe: FT-tagged batches must skip the standard cuda-graph
+            # and route through BreakableCudaGraphRunner so per-sample hook
+            # captures (FinetuneAccumulator) fire. Inference-only batches
+            # (mask is None) fall through untouched.
+            if (
+                forward_batch.is_finetuning_mask is not None
+                and forward_batch.is_finetuning_mask.any()
+                and self.piecewise_cuda_graph_runner is not None
+                and self.piecewise_cuda_graph_runner.can_run(forward_batch)
+            ):
+                ret = self.piecewise_cuda_graph_runner.replay(
+                    forward_batch,
+                    skip_attn_backend_init=skip_attn_backend_init,
+                    pp_proxy_tensors=pp_proxy_tensors,
+                )
+                return ModelRunnerOutput(logits_output=ret, can_run_graph=True)
+
             # Replay cuda graph if applicable
             if can_run_graph:
                 ret = self.graph_runner.replay(
diff --git a/python/sglang/srt/server_args.py b/python/sglang/srt/server_args.py
index 422a67f..34ca9c8 100644
--- a/python/sglang/srt/server_args.py
+++ b/python/sglang/srt/server_args.py
@@ -844,6 +844,11 @@ class ServerArgs:
     # For msProbe
     msprobe_dump_config: Optional[str] = None
 
+    # Fine-tuning (DeltaServe co-serving)
+    enable_finetuning: bool = False
+    backward_mps_percentage: int = 10
+    finetune_config: Optional[str] = None
+
     def __post_init__(self):
         """
         Orchestrates the handling of various server arguments, ensuring proper configuration and validation.
@@ -6929,6 +6934,25 @@ class ServerArgs:
             help="The path of the JSON configuration file for msProbe. If specified, enables msProbe dump.",
         )
 
+        # Fine-tuning (DeltaServe co-serving)
+        parser.add_argument(
+            "--enable-finetuning",
+            action="store_true",
+            help="Enable DeltaServe co-serving (inference + LoRA finetuning).",
+        )
+        parser.add_argument(
+            "--backward-mps-percentage",
+            type=int,
+            default=ServerArgs.backward_mps_percentage,
+            help="CUDA MPS active-thread percentage for the backward (SFT) process.",
+        )
+        parser.add_argument(
+            "--finetune-config",
+            type=str,
+            default=ServerArgs.finetune_config,
+            help="Path to a YAML file with FinetuneConfig overrides.",
+        )
+
     @classmethod
     def from_cli_args(cls, args: argparse.Namespace):
         args.tp_size = args.tensor_parallel_size
